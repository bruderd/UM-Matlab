function f = test_f(arg, params, choice)

    m = params.numFREEs;
    
%% For testing dfdx
%     x = arg;        
%     u = ones(m,1);  % u is fixed
%     dxdt = zeros(n,1);  % dxdt if fixed
% %     u = [1]';  % u is fixed
% %     dxdt = [1 5 7 3 7]';  % dxdt is fixed

%% For testing dfdu
%     x = params.x0;        
%     u = arg;            % u is fixed
%     dxdt = zeros(n,1);  % dxdt is fixed
     
%% For testing dfdxdot
    x = params.x0;    % x is fixed    
    u = ones(m,1);    % u is fixed
    dxdt = arg;  

%%
if choice == 1
    x = arg;        
    u = ones(m,1);  % u is fixed
    xdot = ones(6,1);  % dxdt if fixed
elseif choice == 2
    x = ones(6,1);    % x is fixed    
    u = ones(m,1);    % u is fixed
    xdot = arg;  
elseif choice == 3
        
end
            
        
    
%% Set value of output    
    [f, ~, ~, ~] = vf(x, u, xdot, params);

end